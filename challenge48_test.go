package cryptopals

import "testing"

func TestDecryptRsaPaddingOracleComplete(t *testing.T) {
	t.Skip("Skip this test, because it's here only for completeness (it actually runs test for challenge 47)")
	TestDecryptRsaPaddingOracleSimple(t)
}
